% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latency.R
\name{baseline.hazard}
\alias{baseline.hazard}
\title{Estimate the conditional baseline cumulative hazard function}
\usage{
baseline.hazard(y, delta, Z, beta, w)
}
\arguments{
\item{y}{A numeric vector (length \code{n}) of the observed event time.}

\item{delta}{A numeric vector (length \code{n}) of the censoring indicator.}

\item{Z}{A numeric matrix (dimension \code{n} x \code{q};
each row is an observation vector) of covariates for the latency (no intercept term).}

\item{beta}{A numeric vector (length \code{q}) of the
initial latency regression parameter value.}

\item{w}{A numeric vector (length \code{n}) of the
conditional expected uncured status from the E-step, see \code{\link{em.estep}}.}
}
\value{
A list with components: \describe{
  \item{y.star}{A vector of the distinct uncensored event times in ascending order.}
  \item{hazard}{A vector of the conditional baseline hazard estimates.}
  \item{cum.hazard}{A vector of the conditional baseline cumulative hazard estimates.}
}
}
\description{
Estimate the conditional baseline cumulative hazard function, \eqn{\Lambda(t)}.
}
\keyword{internal}
